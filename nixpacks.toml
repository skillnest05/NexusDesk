[phases.setup]
nixPkgs = ["php83", "php83Extensions.pdo_mysql", "php83Extensions.curl", "php83Extensions.mbstring", "php83Extensions.openssl"]

[phases.build]
cmds = ["mkdir -p /app/uploads"]

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t /app index.php"
